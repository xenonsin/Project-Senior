namespace Senior.Globals
{
    public enum GameState
    {
        MainMenu,
        CharacterSelect,
        Playing,
        Loading,
    }

    public enum PlayerState
    {
        ChoosingCharacter,
        ConfirmedCharacter,
        Playing,
        WaitingForCredits,
    }
}